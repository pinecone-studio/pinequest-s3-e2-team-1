{
  "name": "book-question-backend",
  "version": "1.0.0",
  "private": true,
  "description": "PDF upload + Ollama question generation backend",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "frontend:install": "cd frontend-react && bun install",
    "frontend:dev": "cd frontend-react && bun run dev",
    "frontend:build": "cd frontend-react && bun run build",
    "frontend:preview": "cd frontend-react && bun run preview"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.916.0",
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
